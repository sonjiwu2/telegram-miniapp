// Лимиты вынесены в конфиг, чтобы менять без переписывания логики (раздел 38 ТЗ).
// Общий дневной лимит на все обращения к AI-провайдеру (AI Вердикт + AI-судья
// в спорах) — считается одной и той же таблицей AIRequest.
export const AI_DAILY_FREE_LIMIT = 3;
