generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Модели домена появятся начиная с Этапа 2 (User) и Этапа 4 (Session domain).
